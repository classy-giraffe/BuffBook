---
/**
 * Injects a CSRF token hidden field into forms.
 * Reads the token from Astro.locals (set by middleware).
 */
const token = Astro.locals.csrfToken;
---

{token && <input type="hidden" name="csrf_token" value={token} />}
